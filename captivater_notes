take from their api
download to our server, pull all the images at once, one page render
fake version of the database, have like a 100

paperclip, files attachment gem

have 5 blocks of 
a bucket on S3

in javascript make an array of all the paths 

on a 5 second timer
ajax download new images


then serve the image

read all the text and parse it for matches, build paths for we get all the paths at once 



rails app with devise, figaro
on the index have a box that takes a word and makes it appear at once